# AI 白酒研究教练：界面规范

## 设计定位

- 产品角色：研究教练型工作台，不是营销落地页，也不是图表墙。
- 叙事顺序：先看懂产业逻辑 → 判断行业位置 → 跟踪变化 → 研究公司。
- 功能结构：研究总览 → 行业分析框架 → 核心数据跟踪 → 专家观点演化 → 公司对比。
- 视觉温度：克制、可信、机构研究感。
- 参考锚点：`hedicxl.cn` 的蓝白研究终端语言；只参考视觉系统，不复制其品牌标识、内容或个人信息。
- 结构参考：氨纶仪表盘的“判断优先、验证阈值、同口径比较”，不照搬其暖色视觉或长页导航。

## 品牌与资产边界

- 原型使用纯文字产品名“白酒研究 / AI RESEARCH COACH”。
- 不使用、不仿制工银瑞信、国海证券、hedicxl.cn 或酒企 Logo。
- 当前没有外部图片、图标或品牌资产；页面无热链资源，可离线运行。
- 所有图表均由本地 HTML/CSS/SVG 构成，且明确标注为演示结构。

## 颜色

| 角色 | 色值 |
|---|---|
| 主色 | `#0f5ea8` |
| 主色深 | `#0a4581` |
| 主色浅底 | `#dde9fa` / `#eef5fc` |
| 页面底 | `#f1f6fc` |
| 纸张/卡片 | `#ffffff` |
| 主文字 | `#0b1b2f` |
| 次文字 | `#2a3f5c` / `#56708d` / `#5e7388` |
| 细边框 | `#cfdcec` |
| 强边框 | `#b4c5dc` |
| 支持证据 | `#0d6c44`，必须同时写“支持” |
| 反向证据 | `#a8331f`，必须同时写“反向证据” |
| 待接入/提醒 | `#8b5e14`，必须同时写状态文字 |

颜色不单独承担含义；状态必须配合文字、符号或位置表达。

## 字体

- 界面与标题：`PingFang SC` / `Microsoft YaHei` / 同类中文无衬线。
- 研究摘要与解释：`Kaiti SC` / `STKaiti` / `KaiTi`，只用于有限的编辑感文本。
- 数字、日期、状态与英文标签：`SFMono-Regular` / `JetBrains Mono` / `Menlo` / `Consolas`。
- 不下载网络字体，保证离线与内网环境可用。

## 几何与层级

- 基础间距：4px；常用 8 / 12 / 16 / 24 / 32px。
- 面板圆角：5px；控件圆角：3px；只有状态标签使用胶囊形。
- 常态使用 1px 细边框，无厚重阴影；只有抽屉、搜索框和 Tweaks 使用轻阴影。
- 桌面左侧导航 222px，顶栏 56px；内容按 12 栏思路组织。
- 移动端触控目标不小于 44px，因果图改为纵向链路。

## 动效

- 进入与抽屉使用 180–260ms 的克制过渡。
- 不使用视差、弹跳或无意义的装饰动画。
- 支持系统 `prefers-reduced-motion`，并可在 Tweaks 中主动关闭动效。

## 数据呈现契约

- 顶部常驻来源日期提示，不能关闭。
- 价格曲线必须同时显示单位、截止月份和 Excel 来源。
- 待接入字段使用“待接入”或“—”，不使用 `0` 或虚构最新值占位。
- 真实数据接入后，每个值必须携带：来源、截至日期、单位、频率、口径、质量说明。
- 事实、专家观点、人工归因、AI 推演在视觉和数据结构上分离。
- AI 综合必须区分价格事实、未独立核验渠道调研、报告观点与预测。
- 发生口径冲突时保留两套来源说明，并明确指定网页使用的主口径。
